#include<stdio.h>
int main()
{
	int n,i,j,k,min,temp;
	scanf("%d",&n);
	int arr[n];
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	min=arr[0];
	for(i=0;i<n-1;i++)
	{
		min=i;
		for(j=i+1;j<n;j++)					//搜索最小项 
		{
			if(arr[j]<arr[min])
				min=j;
		}
		if(min!=i)							//调序 
		{
			temp=arr[i]; 
			arr[i]=arr[min];
			arr[min]=temp;
		}
		for(k=0;k<n;k++)
			printf("%d ",arr[k]);
		printf("\n");
	}
	return 0;
}
