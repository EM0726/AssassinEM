#include<stdio.h>
int main()
{
	int i,j,m,n;
	scanf("%d",&n);
	int arr[n];
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	scanf("%d",&m);
	for(i=0;i<n;i++)
	{
		if(m==arr[i])
			printf("%d ",i);
	}
	return 0;
}
